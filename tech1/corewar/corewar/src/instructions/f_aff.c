/*
** EPITECH PROJECT, 2020
** Visual Studio Live Share (Workspace)
** File description:
** f_aff.c
*/

#include "corewar.h"

// 0x10 (aff) takes 1 parameter, which must be a register.
// It displays on the standard output the character
// whose ASCII code is the content of the register (in base 10).
// A 256 modulo is applied to this ASCII code.
// aff r3 displays ’*’ if r3 contains 42.

void f_aff(corewar_t *cw, champs_t *iter)
{
    if (cw || iter) {}
}
